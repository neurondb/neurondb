# NeuronAgent Environment Configuration
# =====================================
# Copy this file to your configuration directory (e.g., /etc/neurondb/neuronagent.env)
# and update the values according to your setup.
#
# Usage:
#   cp scripts/config/neuronagent.env.example /etc/neurondb/neuronagent.env
#   # Edit /etc/neurondb/neuronagent.env with your values
#
# For systemd services, reference this file in your service unit:
#   EnvironmentFile=/etc/neurondb/neuronagent.env

# ---------------------------
# Database Connection
# ---------------------------
# PostgreSQL database connection parameters
# Note: For native PostgreSQL installations, typically use:
#   DB_HOST=localhost
#   DB_PORT=5432
# For Docker Compose setups, typically use:
#   DB_HOST=localhost (or container name in Docker network)
#   DB_PORT=5433 (Docker mapped port)

DB_HOST=localhost
DB_PORT=5432
DB_NAME=neurondb
DB_USER=neurondb
DB_PASSWORD=your_password_here

# Connection pool settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m

# ---------------------------
# Server Configuration
# ---------------------------
# Server bind address
SERVER_HOST=0.0.0.0

# Server port
SERVER_PORT=8080

# Server timeouts
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s

# ---------------------------
# Logging
# ---------------------------
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Log format: json, text
LOG_FORMAT=json

# ---------------------------
# Configuration File
# ---------------------------
# Path to config.yaml configuration file (optional)
# If not set, uses environment variables only
# CONFIG_PATH=/etc/neurondb/config.yaml




