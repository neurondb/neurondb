# NeuronMCP Environment Configuration
# =====================================
# Copy this file to your configuration directory (e.g., /etc/neurondb/neuronmcp.env)
# and update the values according to your setup.
#
# Usage:
#   cp scripts/config/neuronmcp.env.example /etc/neurondb/neuronmcp.env
#   # Edit /etc/neurondb/neuronmcp.env with your values
#
# For systemd services, reference this file in your service unit:
#   EnvironmentFile=/etc/neurondb/neuronmcp.env

# ---------------------------
# Database Connection
# ---------------------------
# PostgreSQL database connection parameters
# Note: For native PostgreSQL installations, typically use:
#   NEURONDB_HOST=localhost
#   NEURONDB_PORT=5432
# For Docker Compose setups, typically use:
#   NEURONDB_HOST=localhost (or container name in Docker network)
#   NEURONDB_PORT=5433 (Docker mapped port)

NEURONDB_HOST=localhost
NEURONDB_PORT=5432
NEURONDB_DATABASE=neurondb
NEURONDB_USER=neurondb
NEURONDB_PASSWORD=your_password_here

# Alternative: Use connection string (overrides above settings)
# Format: postgresql://user:password@host:port/database?sslmode=require
# NEURONDB_CONNECTION_STRING=

# ---------------------------
# Configuration File
# ---------------------------
# Path to mcp-config.json configuration file (optional)
# If not set, uses environment variables only
# NEURONDB_MCP_CONFIG=/etc/neurondb/mcp-config.json

# ---------------------------
# Logging
# ---------------------------
# Log level: debug, info, warn, error
NEURONDB_LOG_LEVEL=info

# Log format: json, text
NEURONDB_LOG_FORMAT=text

# Log output: stdout, stderr, or file path
NEURONDB_LOG_OUTPUT=stderr

# ---------------------------
# Features
# ---------------------------
# Enable GPU acceleration (requires GPU support in NeuronDB)
# Set to true if NeuronDB was built with GPU support
NEURONDB_ENABLE_GPU=false

# ---------------------------
# HTTP Server (Optional)
# ---------------------------
# HTTP server address for metrics endpoint (optional)
# Format: :port or host:port
# If not set, HTTP server is disabled (stdio only)
# NEURONMCP_HTTP_ADDR=:8082




