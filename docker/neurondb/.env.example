# NeuronDB Docker Environment Configuration
# Copy this file to .env and customize for your environment
# 
# Usage:
#   cp .env.example .env
#   # Edit .env with your values
#   docker compose up -d

# =============================================================================
# PostgreSQL Configuration
# =============================================================================

# PostgreSQL superuser
POSTGRES_USER=neurondb

# PostgreSQL password
# WARNING: For production, use Docker secrets or external secrets management
POSTGRES_PASSWORD=neurondb

# Database name
POSTGRES_DB=neurondb

# PostgreSQL host authentication method
# Options: trust, md5, scram-sha-256, password
# For production, use scram-sha-256 or md5
POSTGRES_HOST_AUTH_METHOD=md5

# =============================================================================
# Build Configuration
# =============================================================================

# PostgreSQL major version (16, 17, or 18)
PG_MAJOR=17

# ONNX Runtime version
ONNX_VERSION=1.17.0

# =============================================================================
# GPU Configuration (for GPU variants)
# =============================================================================

# CUDA version (for CUDA variant)
# CUDA_VERSION=12.4.1

# Enable RAPIDS support (0 or 1, for CUDA variant)
# ENABLE_RAPIDS=0

# ROCm version (for ROCm variant)
# ROCM_VERSION=5.7

# Enable GPU acceleration
# NEURONDB_GPU_ENABLED=on

# NVIDIA GPU configuration (for CUDA variant)
# NVIDIA_VISIBLE_DEVICES=all
# NVIDIA_DRIVER_CAPABILITIES=compute,utility

# =============================================================================
# Port Configuration
# =============================================================================

# Host port mapping (CPU variant: 5433, CUDA: 5434, ROCm: 5435, Metal: 5436)
# Configured in docker-compose.yml, not here

# =============================================================================
# SSL/TLS Configuration (Optional)
# =============================================================================

# SSL mode for client connections
# Options: disable, allow, prefer, require, verify-ca, verify-full
# Configure in postgresql.conf or via init scripts

# SSL certificate paths (if using custom certificates)
# SSL_CERT_FILE=/var/lib/postgresql/server.crt
# SSL_KEY_FILE=/var/lib/postgresql/server.key
# SSL_CA_FILE=/var/lib/postgresql/ca.crt

# =============================================================================
# Performance Tuning (Optional)
# =============================================================================

# Shared buffers (PostgreSQL configuration)
# POSTGRES_SHARED_BUFFERS=256MB

# Max connections
# POSTGRES_MAX_CONNECTIONS=100

# Work memory
# POSTGRES_WORK_MEM=4MB

# Maintenance work memory
# POSTGRES_MAINTENANCE_WORK_MEM=64MB
