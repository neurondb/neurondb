# NeuronMCP Environment Configuration
# Copy this file to .env and customize as needed: cp .env.example .env

# ============================================================================
# Database Connection
# ============================================================================
# When connecting from host machine (default):
#   NEURONDB_HOST=localhost
#   NEURONDB_PORT=5433 (Docker mapped port)
#
# When connecting via Docker network (shared network):
#   NEURONDB_HOST=neurondb-cpu (container name)
#   NEURONDB_PORT=5432 (internal container port)
#
# When using Docker Desktop on Mac/Windows, you can use:
#   NEURONDB_HOST=host.docker.internal
#   NEURONDB_PORT=5433
#
# Note: host.docker.internal does NOT work on Linux - use localhost or shared network

NEURONDB_HOST=localhost
NEURONDB_PORT=5433
NEURONDB_DATABASE=neurondb
NEURONDB_USER=neurondb
NEURONDB_PASSWORD=neurondb

# Alternative: Use connection string (overrides individual settings above)
# NEURONDB_CONNECTION_STRING=postgresql://neurondb:neurondb@localhost:5433/neurondb

# ============================================================================
# Logging Configuration
# ============================================================================
# Log levels: debug, info, warn, error
NEURONDB_LOG_LEVEL=info
# Log formats: json, text
NEURONDB_LOG_FORMAT=text
# Log output: stdout, stderr, or file path
NEURONDB_LOG_OUTPUT=stderr

# ============================================================================
# Feature Flags
# ============================================================================
# Enable GPU acceleration (requires GPU-enabled NeuronDB)
NEURONDB_ENABLE_GPU=false

# ============================================================================
# Optional Configuration
# ============================================================================
# Path to mcp-config.json file (inside container)
# NEURONDB_MCP_CONFIG=/app/mcp-config.json
