# NeuronDB Ecosystem - Environment Variables
# Copy this file to .env and customize as needed
# cp .env.example .env
#
# SECURITY: For production, set strong passwords (e.g. openssl rand -base64 32).
# Do not commit .env with real credentials.

# ============================================================================
# NeuronDB Configuration
# ============================================================================
POSTGRES_USER=neurondb
POSTGRES_PASSWORD=change-me
POSTGRES_DB=neurondb
POSTGRES_PORT=5433

# PostgreSQL version (16, 17, or 18)
PG_MAJOR=17

# ONNX Runtime version
ONNX_VERSION=1.17.0

# GPU Configuration (for CUDA/ROCm variants)
# CUDA_VERSION=12.4.1
# ROCM_VERSION=5.7
# ENABLE_RAPIDS=0

# Port mappings for GPU variants (when using profiles)
# POSTGRES_CUDA_PORT=5434
# POSTGRES_ROCM_PORT=5435
# POSTGRES_METAL_PORT=5436

# ============================================================================
# NeuronAgent Configuration
# Automatically connects to NeuronDB via container name
# ============================================================================

# Database connection - uses container name for automatic connection
# For CPU: neurondb-cpu
# For CUDA GPU: neurondb-cuda (set when using --profile cuda)
# For ROCm GPU: neurondb-rocm (set when using --profile rocm)
# For Metal GPU: neurondb-metal (set when using --profile metal)
DB_HOST=neurondb-cpu
DB_PORT=5432
DB_NAME=neurondb
DB_USER=neurondb
DB_PASSWORD=change-me

# Server configuration
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s

# Database connection pool
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m

# Logging
LOG_LEVEL=info
LOG_FORMAT=json

# Optional: Path to config file (inside container)
# CONFIG_PATH=/app/config.yaml

# ============================================================================
# NeuronMCP Configuration
# Automatically connects to NeuronDB via container name
# ============================================================================

# Database connection - uses container name for automatic connection
# For CPU: neurondb-cpu
# For CUDA GPU: neurondb-cuda (set when using --profile cuda)
# For ROCm GPU: neurondb-rocm (set when using --profile rocm)
# For Metal GPU: neurondb-metal (set when using --profile metal)
NEURONDB_HOST=neurondb-cpu
NEURONDB_PORT=5432
NEURONDB_DATABASE=neurondb
NEURONDB_USER=neurondb
NEURONDB_PASSWORD=change-me

# Alternative: use connection string (overrides individual settings)
# NEURONDB_CONNECTION_STRING=postgresql://neurondb:neurondb@neurondb-cpu:5432/neurondb

# Optional: path to config file (inside container)
# NEURONDB_MCP_CONFIG=/app/mcp-config.json

# Logging
NEURONDB_LOG_LEVEL=info
NEURONDB_LOG_FORMAT=text
NEURONDB_LOG_OUTPUT=stderr

# Feature flags
NEURONDB_ENABLE_GPU=false

# ============================================================================
# Build Configuration (optional)
# ============================================================================
# VERSION=latest
# BUILD_DATE=
# VCS_REF=

# ============================================================================
# Maintainer: Docker Hub push (scripts/docker-release.sh only; not used by compose)
# ============================================================================
# Create an access token at https://hub.docker.com/settings/security (Read, Write, Delete).
# GitHub Actions: add repository secrets DOCKERHUB_USERNAME and DOCKERHUB_TOKEN (see docs/release-docker.md).
# DOCKERHUB_USERNAME=your-dockerhub-username
# DOCKERHUB_TOKEN=dckr_pat_...
