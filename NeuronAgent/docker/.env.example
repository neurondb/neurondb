# NeuronAgent Environment Configuration
# Copy this file to .env and customize as needed: cp .env.example .env

# ============================================================================
# Database Connection
# ============================================================================
# When connecting from host machine (default):
#   DB_HOST=localhost
#   DB_PORT=5433 (Docker mapped port)
#
# When connecting via Docker network (shared network):
#   DB_HOST=neurondb-cpu (container name)
#   DB_PORT=5432 (internal container port)
#
# When using Docker Desktop on Mac/Windows:
#   DB_HOST=host.docker.internal
#   DB_PORT=5433

DB_HOST=localhost
DB_PORT=5433
DB_NAME=neurondb
DB_USER=neurondb
DB_PASSWORD=neurondb

# Database Connection Pool Settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5
DB_CONN_MAX_LIFETIME=5m

# ============================================================================
# Server Configuration
# ============================================================================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_READ_TIMEOUT=30s
SERVER_WRITE_TIMEOUT=30s

# ============================================================================
# Logging Configuration
# ============================================================================
# Log levels: debug, info, warn, error
LOG_LEVEL=info
# Log formats: json, text
LOG_FORMAT=json

# ============================================================================
# Optional Configuration
# ============================================================================
# Path to config.yaml file (optional, mounts inside container)
# CONFIG_PATH=/app/configs/config.yaml

# Authentication API key header name (optional)
# AUTH_API_KEY_HEADER=X-API-Key
